# Used only by the backend. Put the real key in a local .env file or a Cloud Run secret.
OPENROUTER_API_KEY=

# Override only when you intentionally want a different model for readability decisions.
PAPER_INSPECT_MODEL=deepseek/deepseek-v4-flash
